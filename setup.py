from distutils.core import setup
setup(
  name = 'QueueBuffer',
  packages = ['QueueBuffer'],
  version = '1.01',
  description = 'A buffer/queue in which the last few items are available for accessing using stable indices.',
  author = 'Mike Smith',
  author_email = 'm.t.smith@sheffield.ac.uk',
  url = 'https://github.com/lionfish0/QueueBuffer.git',
  download_url = 'https://github.com/lionfish0/QueueBuffer.git',
  keywords = ['queue','buffer'],
  classifiers = [],
  install_requires=[],
)
